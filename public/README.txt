Static assets go here — Vite serves everything in this folder from the site root.

Add:
- resume.pdf   -> makes the Resume window's download button work (currently 404s without it)
- favicon.ico  -> optional, add a matching <link> tag in index.html if you add one
